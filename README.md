# Required softwares
1. Python 3.10+
2. Node 22.13
3. MongoDB 8.0.4

# Install the required libraries using the following command
pip install -r requirements.txt

# To start the BE server run the following command
python run.py